<%= form_for(tag) do |f| %>
  <% if tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :required => 'required'%>
  </div>

</br>

  <div class="field">
    <%= f.label :status %>
</br>
    <%= f.radio_button(:status, "Active") %>
    <%= f.label(:status_active, "Active", :style => "display:inline") %><br>
    <%= f.radio_button(:status, "Suspended") %>
    <%= f.label(:status_active, "Suspended", :style => "display:inline") %>

        <!--<%= f.text_field :status %>-->
  </div>


<!--ALEXH TYPE NOT USEFUL AT THE MOMENT - 21.05.17
  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>
-->
</br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
