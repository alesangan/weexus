</br>
<p id="notice"><%= notice %></p>

<h1 class="mytitle">Our Excluded Words</h1>
</br>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Words</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @exclusions.each do |exclusion| %>
      <tr>
        <td><%= exclusion.word %></td>

        <!-- <td><%= link_to 'Show', exclusion %></td> -->
        <td><%= link_to icon('pencil-square-o'), edit_exclusion_path(exclusion) %></td>
        <td><%= link_to  link_to icon('trash-o'), exclusion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Exclusion', new_exclusion_path %>
<!-- renderer:BootstrapPagination::Rails -->
<%= will_paginate @posts,renderer:BootstrapPagination::Rails, inner_window: 1,outer_window: 0%>
